<%- include('partials/header', { title: '404 - Page Not Found' }) %>

    <div class="container">
        <div class="not-found">
            <h1>404</h1>
            <h2>Page Not Found</h2>
            <p>
                Oops! The page <code
                    style="background:var(--card-bg);padding:0.2rem 0.5rem;border-radius:4px"><%= url %></code><br />
                doesn't exist or has been moved.
            </p>
            <div class="flex gap-1" style="justify-content:center">
                <a href="/" class="btn btn-primary">🏠 Go Home</a>
                <a href="/blog" class="btn btn-outline">📝 Read Blog</a>
            </div>
            <div class="card mt-2"
                style="background:rgba(108,99,255,0.07);border-color:rgba(108,99,255,0.25);max-width:500px">
                <p style="font-size:0.85rem;color:var(--text-muted)">
                    💡 <strong>Exercise 4 — Custom 404:</strong>
                    This page is rendered by a catch-all <code>app.use()</code> middleware that calls
                    <code>res.status(404).render('404')</code>.
                </p>
            </div>
        </div>
    </div>

    <%- include('partials/footer') %>